package ru.starimg.ai.ui.theme

import androidx.compose.ui.graphics.Color

/** Brand colors referenced from the XML theme so both stay in one place. */
object Brand {
    val accent = Color(0xFF7C5CFC)
    val bar = Color(0xFF0C0B11)
}
